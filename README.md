# kubectl-easy-install
To install kubectl with single command:
```
sudo apt install git \
&& git clone https://github.com/shahinam2/kubectl-easy-install.git \
cd kubectl-easy-install
chmod +x kubectl-install.sh
./kubectl-install.sh
```
